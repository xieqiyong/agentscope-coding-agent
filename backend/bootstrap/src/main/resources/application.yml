spring:
  application:
    name: agent-platform

  datasource:
    url: jdbc:mysql://192.168.50.105:3306/agent_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Xqy20250513

  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      # 开发阶段用 update 自动建表；稳定后建议切换到 Flyway/Liquibase。
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        jdbc:
          time_zone: Asia/Shanghai

  sql:
    init:
      mode: never

server:
  port: 8081

logging:
  level:
    com.agentplatform: DEBUG
    org.hibernate.SQL: WARN
